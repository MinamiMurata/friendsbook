<% flash.each do |key, value| %>
  <%= content_tag(:div, value, class: "#{key}") %>
<% end %>

<h1><%= @user.name %>のページ</h1>

<p class="mt-4">メールアドレス: <%= @user.email %></p>

<%= link_to 'Back', feeds_path, class: "mt-2 btn btn-outline-info btn-lg" %>